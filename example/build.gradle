plugins {
    id 'com.github.johnrengelman.shadow' version '6.0.0'
    id 'java-library'
}

dependencies {
    api project(':core')
}

build {
    dependsOn {
        shadowJar {
            archiveName('NPCRanking.jar')
        }
    }
}